@using PureRide.Web.Controllers.Account
@model PureRide.Web.ViewModels.Account.AccountEmailModel
@{this.RetrieveModel();}

@using (Html.BeginUmbracoForm<RegisterController>("PreRegisterFormSubmit"))
{
    @Html.AntiForgeryToken()

    @Html.ValidationMessageFor(m => m.Email)
    
    @Html.LabelFor(m => Model.Email)
    <input type="email" required name="email" id="emailaddress" autocomplete="email" maxlength="@Exerp.Api.Constants.MaxLengthEmail" />

    <input type="submit" class="btn btn--primary" />
}